using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class GameLoader : MonoBehaviour
{

	// Load here all content to generate the characters, then store it on some save game or another json file
	// Get json files load of contents to generate, generated content then is loaded into dynamic external files with names and connected hours and distanced etc


	public List<GDUserName> MaleNames = new List<GDUserName>();
	public List<GDUserName> FemaleNames = new List<GDUserName>();

	// Start is called before the first frame update
	void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}

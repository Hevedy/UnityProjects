using UnityEngine;

public class GWProp : GWInteractable {

	public GDProp Data = null;

	// Start is called before the first frame update
	void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}

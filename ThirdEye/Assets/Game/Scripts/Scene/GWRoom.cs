using System.Collections.Generic;
using UnityEngine;

public class GWRoom : MonoBehaviour
{

	public GDRoom Data = null;

	public List<GWProp> PropsList = new List<GWProp>();
	public List<EntityRef> PropsListRef = new List<EntityRef>(); //Load here base then the rest

	// Start is called before the first frame update
	void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
